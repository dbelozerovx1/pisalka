#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

export RUST_LOG="${RUST_LOG:-info}"
export FLIGHT_ADDR="${FLIGHT_ADDR:-0.0.0.0:50051}"
export METRICS_ENABLED="${METRICS_ENABLED:-true}"
export METRICS_ADDR="${METRICS_ADDR:-0.0.0.0:9090}"
export WORKER_ID="${WORKER_ID:-local-worker}"
export WORKER_FLIGHT_URI="${WORKER_FLIGHT_URI:-http://127.0.0.1:50051}"
export WORKER_ZONE="${WORKER_ZONE:-}"
export WORKER_DRAINING="${WORKER_DRAINING:-false}"
export WORKER_REQUIRE_STRUCTURED_TICKETS="${WORKER_REQUIRE_STRUCTURED_TICKETS:-false}"
export WORKER_HEARTBEAT_INTERVAL_MS="${WORKER_HEARTBEAT_INTERVAL_MS:-5000}"
export WORKER_REGISTRY_TTL_MS="${WORKER_REGISTRY_TTL_MS:-15000}"
export WORKER_REQUIRE_SIGNED_CAPABILITIES="${WORKER_REQUIRE_SIGNED_CAPABILITIES:-false}"
export WORKER_CAPABILITY_SECRET="${WORKER_CAPABILITY_SECRET:-}"
export WORKER_REQUIRE_CAPABILITY_WORKER_ID="${WORKER_REQUIRE_CAPABILITY_WORKER_ID:-}"
export WORKER_CAPABILITY_MAX_TTL_MS="${WORKER_CAPABILITY_MAX_TTL_MS:-3600000}"
export WORKER_MEMORY_BYTES="${WORKER_MEMORY_BYTES:-}"
export WORKER_RESERVED_MEMORY_BYTES="${WORKER_RESERVED_MEMORY_BYTES:-}"
export PUT_MEMORY_PERCENT="${PUT_MEMORY_PERCENT:-55}"
export PUT_MEMORY_BUDGET_BYTES="${PUT_MEMORY_BUDGET_BYTES:-}"
export PUT_MAX_STREAM_MEMORY_BYTES="${PUT_MAX_STREAM_MEMORY_BYTES:-}"
export PUT_MAX_RECORD_BATCH_BYTES="${PUT_MAX_RECORD_BATCH_BYTES:-}"
export READ_MEMORY_PERCENT="${READ_MEMORY_PERCENT:-30}"
export READ_MEMORY_BUDGET_BYTES="${READ_MEMORY_BUDGET_BYTES:-}"
export READ_MAX_STREAM_MEMORY_BYTES="${READ_MAX_STREAM_MEMORY_BYTES:-}"
export READ_MAX_RECORD_BATCH_BYTES="${READ_MAX_RECORD_BATCH_BYTES:-}"
export READ_MAX_BATCH_ROWS="${READ_MAX_BATCH_ROWS:-1048576}"
export S3_ENDPOINT="${S3_ENDPOINT:-http://127.0.0.1:9000}"
export S3_BUCKET="${S3_BUCKET:-arrow-flight}"
export S3_REGION="${S3_REGION:-us-east-1}"
export AWS_ACCESS_KEY_ID="${AWS_ACCESS_KEY_ID:-minioadmin}"
export AWS_SECRET_ACCESS_KEY="${AWS_SECRET_ACCESS_KEY:-minioadmin}"
export AWS_ALLOW_HTTP="${AWS_ALLOW_HTTP:-true}"
export PARQUET_COMPRESSION="${PARQUET_COMPRESSION:-snappy}"
export PARQUET_DICTIONARY="${PARQUET_DICTIONARY:-false}"
export S3_MULTIPART_PART_SIZE="${S3_MULTIPART_PART_SIZE:-67108864}"
export S3_MULTIPART_MAX_CONCURRENCY="${S3_MULTIPART_MAX_CONCURRENCY:-16}"
export PUT_PARALLELISM="${PUT_PARALLELISM:-4}"
export PUT_QUEUE_DEPTH="${PUT_QUEUE_DEPTH:-2}"
export PUT_MAX_ACTIVE_STREAMS="${PUT_MAX_ACTIVE_STREAMS:-16}"
export PUT_MAX_STREAMS_PER_UPLOAD="${PUT_MAX_STREAMS_PER_UPLOAD:-8}"
export PUT_SCHEDULER_RESERVED_SLOTS="${PUT_SCHEDULER_RESERVED_SLOTS:-0}"
export PUT_SLOT_WAIT_MS="${PUT_SLOT_WAIT_MS:-30000}"
export PUT_FIRST_BATCH_TIMEOUT_MS="${PUT_FIRST_BATCH_TIMEOUT_MS:-10000}"
export PUT_MAX_STREAM_BYTES="${PUT_MAX_STREAM_BYTES:-}"
export PUT_REQUIRE_STAGING_PREFIX="${PUT_REQUIRE_STAGING_PREFIX:-false}"
export READ_MAX_ACTIVE_STREAMS="${READ_MAX_ACTIVE_STREAMS:-16}"
export READ_MAX_STREAMS_PER_OPERATION="${READ_MAX_STREAMS_PER_OPERATION:-8}"
export READ_SCHEDULER_RESERVED_SLOTS="${READ_SCHEDULER_RESERVED_SLOTS:-0}"
export READ_SLOT_WAIT_MS="${READ_SLOT_WAIT_MS:-30000}"
export READ_BATCH_SIZE="${READ_BATCH_SIZE:-65536}"
export FLIGHT_MAX_MESSAGE_SIZE="${FLIGHT_MAX_MESSAGE_SIZE:-268435456}"
export FLIGHT_DATA_CHUNK_SIZE="${FLIGHT_DATA_CHUNK_SIZE:-16777216}"

cargo run --release --bin flight-server
